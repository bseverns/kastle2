## StackBrain Artifacts

This directory holds publication-safe build artifacts for `StackBrain`.

Current tracked file:
- `kastle2-stackbrain.uf2`

Notes:
- this firmware was built from the `StackBrain` app in `code/src/apps/StackBrain/`
- the source of truth remains the app code and docs, not this binary
- local build trees and downloaded toolchains under `build*` and `tools/` are intentionally ignored
